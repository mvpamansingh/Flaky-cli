{
  "name": "sample-flaky-project",
  "version": "0.0.0",
  "private": true,
  "description": "Deliberately unreliable test suite — fixture for building Flaky-test Detective against. Do not fix these tests; they flake on purpose.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.2"
  }
}